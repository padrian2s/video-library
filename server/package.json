{
  "name": "url-collector-server",
  "version": "1.0.0",
  "description": "API service to collect URLs from Chrome extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=3000 node --watch server.js",
    "generate-key": "node -e \"console.log(require('crypto').randomUUID())\""
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
